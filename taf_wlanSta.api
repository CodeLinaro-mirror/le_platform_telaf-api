/**
 * Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
 * SPDX-License-Identifier: BSD-3-Clause-Clear
 **/
//------------------------------------------------------------------------------------------------
/**
 * @page c_tafwlansta Wireless LAN Station Management Service
 *
 * @rst :ref:`API reference <File taf_wlanSta_interface.h>` @endrst
 *
 * <HR>
 *
 * The wireless LAN (WLAN) station (STA) management service provides APIs for
 * applications to manage WLAN stations that connect to wireless access points. This service
 * enables devices to act as WLAN clients, connecting to existing wireless networks and
 * accessing network resources.
 *
 * The service supports advanced features including access point scanning, multiple security
 * modes (WPA/WPA2/WPA3), automatic IP configuration, connection state monitoring, and
 * router and bridge modes for network integration.
 *
 * @b NOTE: Before using this service, the WLAN device management service must be
 * initialized and configured a mode that includes STA functionality.
 *
 * @section taf_wlansta_binding IPC interfaces binding
 *
 * The functions of this service are provided by the @b tafWlanSvc application service.
 *
 * The following example illustrates how to bind to the WLAN Station service.
 * @verbatim
   bindings:
   {
       clientExe.clientComponent.taf_wlanSta -> tafWlanSvc.taf_wlanSta
   }
   @endverbatim
 *
 * @section c_taf_wlan_sta_basic_setup Set up a basic station
 *
 * The following example demonstrates how to set up a basic WLAN STA:
 *
 * @code
 *
   // First, ensure WLAN device is configured for STA mode
   le_result_t result = taf_wlan_TurnOn(NULL);
   if (result != LE_OK) {
       LE_ERROR("Failed to turn on WLAN device");
       return result;
   }
 *
   result = taf_wlan_SetMode(NULL, TAF_WLAN_MODE_STA);
   if (result != LE_OK) {
       LE_ERROR("Failed to set STA mode");
       return result;
   }
 *
   // Get STA interface information
   taf_wlan_APIntfInfo_t apIntf[TAF_WLAN_MAX_NUM_AP];
   taf_wlan_STAIntfInfo_t staIntf[TAF_WLAN_MAX_NUM_STA];
   size_t apIntfSize = TAF_WLAN_MAX_NUM_AP;
   size_t staIntfSize = TAF_WLAN_MAX_NUM_STA;
 *
   result = taf_wlan_GetIntfInfo(NULL, apIntf, &apIntfSize, staIntf, &staIntfSize);
   if (result != LE_OK || staIntfSize == 0) {
       LE_ERROR("No STA interfaces available");
       return LE_FAULT;
   }
 *
   // Get STA reference
   taf_wlanSta_WlanSTARef_t staRef = taf_wlanSta_GetWlanSTA(staIntf[0].id,
                                                            staIntf[0].IntfName);
   if (staRef == NULL) {
       LE_ERROR("Failed to get STA reference");
       return LE_FAULT;
   }
 *
   LE_INFO("STA reference obtained for interface: %s", staIntf[0].IntfName);
 *
   @endcode
 *
 * @section c_taf_wlan_sta_scanning Scan for available access points
 *
 * Before connecting to an access point, scan for available networks:
 *
 * @code
 *
   static le_sem_Ref_t scanSemRef = NULL;
   static taf_wlanSta_EventHandlerRef_t staEventHandlerRef = NULL;
 *
   // Station event handler
   static void StationEventHandler(
       taf_wlanSta_WlanSTARef_t wlanSTARef,
       taf_wlanSta_State_t staState,
       void* contextPtr
   )
   {
       switch (staState) {
           case TAF_WLANSTA_STATE_SCAN_STARTED:
               LE_INFO("AP scan started");
               break;
           case TAF_WLANSTA_STATE_SCAN_COMPLETED:
               LE_INFO("AP scan completed");
               le_sem_Post(scanSemRef);
               break;
           case TAF_WLANSTA_STATE_SCAN_FAILED:
               LE_ERROR("AP scan failed");
               le_sem_Post(scanSemRef);
               break;
           default:
               LE_INFO("Station state changed: %d", staState);
               break;
       }
   }
 *
   le_result_t ScanForAccessPoints(taf_wlanSta_WlanSTARef_t staRef)
   {
       le_result_t result;
 *
       // Create semaphore for scan synchronization
       scanSemRef = le_sem_Create("ScanSem", 0);
 *
       // Register for station events
       staEventHandlerRef = taf_wlanSta_AddEventHandler(staRef, StationEventHandler, NULL);
       if (staEventHandlerRef == NULL) {
           LE_ERROR("Failed to register station event handler");
           return LE_FAULT;
       }
 *
       // Start AP scan
       result = taf_wlanSta_DoAPScan(staRef);
       if (result != LE_OK) {
           LE_ERROR("Failed to start AP scan: %d", result);
           return result;
       }
 *
       // Wait for scan completion
       le_sem_Wait(scanSemRef);
 *
       // Get scan results
       uint16_t numAPs = 0;
       size_t apInfoSize = TAF_WLANSTA_MAX_APSCAN_RESULT_NUM;
       taf_wlanSta_APInfo_t apInfo[TAF_WLANSTA_MAX_APSCAN_RESULT_NUM];
 *
       result = taf_wlanSta_GetAPScanResults(staRef, &numAPs, apInfo, &apInfoSize);
       if (result == LE_OK) {
           LE_INFO("Found %d access points:", numAPs);
           for (size_t i = 0; i < numAPs && i < apInfoSize; i++) {
               LE_INFO("  AP %zu:", i + 1);
               LE_INFO("    SSID: %s", apInfo[i].SSID);
               LE_INFO("    BSSID: %s", apInfo[i].BSSID);
               LE_INFO("    Signal: %d dBm", apInfo[i].SignalLevel);
               LE_INFO("    Frequency: %d MHz", apInfo[i].Frequency);
               LE_INFO("    Security: %d", apInfo[i].secMode);
           }
       }
 *
       return result;
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_connection Connect to an access point
 *
 * @subsection c_taf_wlan_sta_connect_secure Connect to a secure access point
 *
 * Connect to a WPA2-PSK secured access point:
 *
 * @code
 *
   static std::promise<taf_wlanSta_State_t> connectPromise;
 *
   static void ConnectionEventHandler(
       taf_wlanSta_WlanSTARef_t wlanSTARef,
       taf_wlanSta_State_t staState,
       void* contextPtr
   )
   {
       switch (staState) {
           case TAF_WLANSTA_STATE_CONNECTING:
               LE_INFO("Connecting to access point...");
               break;
           case TAF_WLANSTA_STATE_CONNECTED:
               LE_INFO("Successfully connected to access point");
               connectPromise.set_value(TAF_WLANSTA_STATE_CONNECTED);
               break;
           case TAF_WLANSTA_STATE_ASSOCIATION_FAILED:
               LE_ERROR("Failed to associate with access point");
               connectPromise.set_value(TAF_WLANSTA_STATE_ASSOCIATION_FAILED);
               break;
           case TAF_WLANSTA_STATE_IP_ASSIGNMENT_FAILED:
               LE_ERROR("Failed to get IP address via DHCP");
               connectPromise.set_value(TAF_WLANSTA_STATE_IP_ASSIGNMENT_FAILED);
               break;
           default:
               break;
       }
   }
 *
   le_result_t ConnectToSecureAP(taf_wlanSta_WlanSTARef_t staRef,
                                 const char* ssid,
                                 const char* password)
   {
       le_result_t result;
 *
       // Find the target AP in scan results
       uint16_t numAPs = 0;
       size_t apInfoSize = TAF_WLANSTA_MAX_APSCAN_RESULT_NUM;
       taf_wlanSta_APInfo_t apInfo[TAF_WLANSTA_MAX_APSCAN_RESULT_NUM];
       taf_wlanSta_APInfo_t targetAP;
       bool apFound = false;
 *
       result = taf_wlanSta_GetAPScanResults(staRef, &numAPs, apInfo, &apInfoSize);
       if (result != LE_OK) {
           LE_ERROR("Failed to get scan results");
           return result;
       }
 *
       // Search for target SSID
       for (size_t i = 0; i < numAPs && i < apInfoSize; i++) {
           if (strcmp(apInfo[i].SSID, ssid) == 0) {
               targetAP = apInfo[i];
               apFound = true;
               LE_INFO("Found target AP: %s", ssid);
               break;
           }
       }
 *
       if (!apFound) {
           LE_ERROR("Target AP '%s' not found in scan results", ssid);
           return LE_NOT_FOUND;
       }
 *
       // Set WPA2 PSK if required
       if (targetAP.secAuthMethod == TAF_WLAN_SEC_AUTH_METHOD_PSK) {
           result = taf_wlanSta_SetWpa2Psk(staRef, &targetAP, password);
           if (result != LE_OK) {
               LE_ERROR("Failed to set WPA2 PSK: %d", result);
               return result;
           }
           LE_INFO("WPA2 PSK configured for AP: %s", ssid);
       }
 *
       // Register connection event handler
       taf_wlanSta_EventHandlerRef_t eventHandlerRef =
           taf_wlanSta_AddEventHandler(staRef, ConnectionEventHandler, NULL);
 *
       // Connect to the access point
       connectPromise = std::promise<taf_wlanSta_State_t>();
       result = taf_wlanSta_Connect(staRef, &targetAP);
       if (result != LE_OK) {
           LE_ERROR("Failed to initiate connection: %d", result);
           return result;
       }
 *
       // Wait for connection result (timeout after 60 seconds)
       auto future = connectPromise.get_future();
       auto status = future.wait_for(std::chrono::seconds(60));
 *
       if (status == std::future_status::ready) {
           taf_wlanSta_State_t connectionState = future.get();
           if (connectionState == TAF_WLANSTA_STATE_CONNECTED) {
               LE_INFO("Successfully connected to %s", ssid);
               result = LE_OK;
           } else {
               LE_ERROR("Connection failed with state: %d", connectionState);
               result = LE_FAULT;
           }
       } else {
           LE_ERROR("Connection timeout");
           result = LE_TIMEOUT;
       }
 *
       // Cleanup event handler
       if (eventHandlerRef) {
           taf_wlanSta_RemoveEventHandler(eventHandlerRef);
       }
 *
       return result;
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_ip_configuration Manage IP configurations
 *
 * @subsection c_taf_wlan_sta_dynamic_ip Configure dynamic IPs (DHCP)
 *
 * Configure the station to use DHCP for automatic IP assignment:
 *
 * @code
 *
   le_result_t ConfigureDynamicIP(taf_wlanSta_WlanSTARef_t staRef)
   {
       taf_wlanSta_IPConfig_t ipConfig = {0}; // Not used for dynamic IP
 *
       le_result_t result = taf_wlanSta_SetIPConfig(staRef, TAF_WLANSTA_IPTYPE_DYNAMIC,
                                                    &ipConfig);
       if (result != LE_OK) {
           LE_ERROR("Failed to set dynamic IP configuration: %d", result);
           return result;
       }
 *
       LE_INFO("Station configured for dynamic IP (DHCP)");
       return LE_OK;
   }
 *
   @endcode
 *
 * @subsection c_taf_wlan_sta_static_ip Configure static IPs
 *
 * Configure the station with static IP settings:
 *
 * @code
 *
   le_result_t ConfigureStaticIP(taf_wlanSta_WlanSTARef_t staRef)
   {
       taf_wlanSta_IPConfig_t ipConfig;
       memset(&ipConfig, 0, sizeof(ipConfig));
 *
       // Set static IP configuration
       strncpy(ipConfig.IPv4Addr, "192.168.1.100", sizeof(ipConfig.IPv4Addr) - 1);
       strncpy(ipConfig.GWAddr, "192.168.1.1", sizeof(ipConfig.GWAddr) - 1);
       strncpy(ipConfig.DNSAddr, "8.8.8.8", sizeof(ipConfig.DNSAddr) - 1);
       strncpy(ipConfig.NetMask, "255.255.255.0", sizeof(ipConfig.NetMask) - 1);
 *
       le_result_t result = taf_wlanSta_SetIPConfig(staRef, TAF_WLANSTA_IPTYPE_STATIC,
                                                    &ipConfig);
       if (result != LE_OK) {
           LE_ERROR("Failed to set static IP configuration: %d", result);
           return result;
       }
 *
       LE_INFO("Station configured with static IP:");
       LE_INFO("  IP Address: %s", ipConfig.IPv4Addr);
       LE_INFO("  Gateway: %s", ipConfig.GWAddr);
       LE_INFO("  DNS: %s", ipConfig.DNSAddr);
       LE_INFO("  Netmask: %s", ipConfig.NetMask);
 *
       return LE_OK;
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_connection_management Manage connections
 *
 * @subsection c_taf_wlan_sta_complete_workflow Complete connection workflow
 *
 * The following example demonstrates a complete station connection workflow:
 *
 * @code
 *
   typedef struct {
       taf_wlanSta_WlanSTARef_t staRef;
       taf_wlanSta_EventHandlerRef_t eventHandlerRef;
       le_sem_Ref_t scanSemRef;
       le_sem_Ref_t connectSemRef;
       taf_wlanSta_State_t lastState;
   } STAContext_t;
 *
   static STAContext_t staContext = {0};
 *
   static void StationEventHandler(
       taf_wlanSta_WlanSTARef_t wlanSTARef,
       taf_wlanSta_State_t staState,
       void* contextPtr
   )
   {
       STAContext_t* ctx = (STAContext_t*)contextPtr;
       ctx->lastState = staState;
 *
       switch (staState) {
           case TAF_WLANSTA_STATE_SCAN_STARTED:
               LE_INFO("Access point scan started");
               break;
           case TAF_WLANSTA_STATE_SCAN_COMPLETED:
               LE_INFO("Access point scan completed");
               le_sem_Post(ctx->scanSemRef);
               break;
           case TAF_WLANSTA_STATE_SCAN_FAILED:
               LE_ERROR("Access point scan failed");
               le_sem_Post(ctx->scanSemRef);
               break;
           case TAF_WLANSTA_STATE_CONNECTING:
               LE_INFO("Connecting to access point...");
               break;
           case TAF_WLANSTA_STATE_CONNECTED:
               LE_INFO("Successfully connected to access point");
               le_sem_Post(ctx->connectSemRef);
               break;
           case TAF_WLANSTA_STATE_DISCONNECTED:
               LE_INFO("Disconnected from access point");
               le_sem_Post(ctx->connectSemRef);
               break;
           case TAF_WLANSTA_STATE_ASSOCIATION_FAILED:
               LE_ERROR("Failed to associate with access point");
               le_sem_Post(ctx->connectSemRef);
               break;
           case TAF_WLANSTA_STATE_IP_ASSIGNMENT_FAILED:
               LE_ERROR("Failed to get IP address via DHCP");
               le_sem_Post(ctx->connectSemRef);
               break;
           default:
               LE_INFO("Unknown station state: %d", staState);
               break;
       }
   }
 *
   le_result_t ConnectToWiFiNetwork(const char* ssid, const char* password)
   {
       le_result_t result;
 *
       // Initialize WLAN device for STA mode
       result = taf_wlan_TurnOn(NULL);
       if (result != LE_OK) {
           LE_ERROR("Failed to turn on WLAN device");
           return result;
       }
 *
       result = taf_wlan_SetMode(NULL, TAF_WLAN_MODE_STA);
       if (result != LE_OK) {
           LE_ERROR("Failed to set STA mode");
           return result;
       }
 *
       // Get STA interface
       taf_wlan_APIntfInfo_t apIntf[TAF_WLAN_MAX_NUM_AP];
       taf_wlan_STAIntfInfo_t staIntf[TAF_WLAN_MAX_NUM_STA];
       size_t apIntfSize = TAF_WLAN_MAX_NUM_AP;
       size_t staIntfSize = TAF_WLAN_MAX_NUM_STA;
 *
       result = taf_wlan_GetIntfInfo(NULL, apIntf, &apIntfSize, staIntf, &staIntfSize);
       if (result != LE_OK || staIntfSize == 0) {
           LE_ERROR("No STA interfaces available");
           return LE_FAULT;
       }
 *
       // Get STA reference
       staContext.staRef = taf_wlanSta_GetWlanSTA(staIntf[0].id, staIntf[0].IntfName);
       if (staContext.staRef == NULL) {
           LE_ERROR("Failed to get STA reference");
           return LE_FAULT;
       }
 *
       // Create semaphores
       staContext.scanSemRef = le_sem_Create("ScanSem", 0);
       staContext.connectSemRef = le_sem_Create("ConnectSem", 0);
 *
       // Register event handler
       staContext.eventHandlerRef = taf_wlanSta_AddEventHandler(staContext.staRef,
                                                                StationEventHandler,
                                                                &staContext);
 *
       // Start the station
       result = taf_wlanSta_Start(staContext.staRef);
       if (result != LE_OK) {
           LE_ERROR("Failed to start station: %d", result);
           return result;
       }
 *
       // Configure for dynamic IP
       taf_wlanSta_IPConfig_t ipConfig = {0};
       result = taf_wlanSta_SetIPConfig(staContext.staRef, TAF_WLANSTA_IPTYPE_DYNAMIC,
                                        &ipConfig);
       if (result != LE_OK) {
           LE_ERROR("Failed to set IP configuration: %d", result);
           return result;
       }
 *
       // Scan for access points
       result = taf_wlanSta_DoAPScan(staContext.staRef);
       if (result != LE_OK) {
           LE_ERROR("Failed to start scan: %d", result);
           return result;
       }
 *
       // Wait for scan completion
       le_sem_Wait(staContext.scanSemRef);
       if (staContext.lastState != TAF_WLANSTA_STATE_SCAN_COMPLETED) {
           LE_ERROR("Scan did not complete successfully");
           return LE_FAULT;
       }
 *
       // Get scan results and find target AP
       uint16_t numAPs = 0;
       size_t apInfoSize = TAF_WLANSTA_MAX_APSCAN_RESULT_NUM;
       taf_wlanSta_APInfo_t apInfo[TAF_WLANSTA_MAX_APSCAN_RESULT_NUM];
       taf_wlanSta_APInfo_t targetAP;
       bool apFound = false;
 *
       result = taf_wlanSta_GetAPScanResults(staContext.staRef, &numAPs, apInfo, &apInfoSize);
       if (result != LE_OK) {
           LE_ERROR("Failed to get scan results");
           return result;
       }
 *
       for (size_t i = 0; i < numAPs && i < apInfoSize; i++) {
           if (strcmp(apInfo[i].SSID, ssid) == 0) {
               targetAP = apInfo[i];
               apFound = true;
               break;
           }
       }
 *
       if (!apFound) {
           LE_ERROR("Target AP '%s' not found", ssid);
           return LE_NOT_FOUND;
       }
 *
       // Set password for secure connection
       if (targetAP.secAuthMethod == TAF_WLAN_SEC_AUTH_METHOD_PSK) {
           result = taf_wlanSta_SetWpa2Psk(staContext.staRef, &targetAP, password);
           if (result != LE_OK) {
               LE_ERROR("Failed to set WPA2 PSK: %d", result);
               return result;
           }
       }
 *
       // Connect to the access point
       result = taf_wlanSta_Connect(staContext.staRef, &targetAP);
       if (result != LE_OK) {
           LE_ERROR("Failed to initiate connection: %d", result);
           return result;
       }
 *
       // Wait for connection result
       le_sem_Wait(staContext.connectSemRef);
       if (staContext.lastState == TAF_WLANSTA_STATE_CONNECTED) {
           LE_INFO("Successfully connected to %s", ssid);

           // Get connection status
           taf_wlanSta_State_t state;
           char intfName[TAF_NET_INTERFACE_NAME_MAX_LEN];
           char ipv4Addr[TAF_NET_IPV4_ADDR_MAX_LEN];
           char ipv6Addr[TAF_NET_IPV6_ADDR_MAX_LEN];
           char macAddr[TAF_NET_MAC_ADDR_MAX_LEN];
 *
           result = taf_wlanSta_GetStatus(staContext.staRef, &state, intfName,
                                          sizeof(intfName), ipv4Addr, sizeof(ipv4Addr),
                                          ipv6Addr, sizeof(ipv6Addr), macAddr,
                                          sizeof(macAddr));
           if (result == LE_OK) {
               LE_INFO("Connection details:");
               LE_INFO("  Interface: %s", intfName);
               LE_INFO("  IPv4 Address: %s", ipv4Addr);
               LE_INFO("  MAC Address: %s", macAddr);
           }

           return LE_OK;
       } else {
           LE_ERROR("Connection failed or timed out");
           return LE_FAULT;
       }
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_mode_management Manage station modes
 *
 * Configure the station to operate in router or bridge mode:
 *
 * @code
 *
   le_result_t ConfigureStationMode(taf_wlanSta_WlanSTARef_t staRef, bool useRouterMode)
   {
       taf_wlanSta_Mode_t mode = useRouterMode ? TAF_WLANSTA_MODE_ROUTER :
                                                 TAF_WLANSTA_MODE_BRIDGE;
 *
       le_result_t result = taf_wlanSta_SetMode(staRef, mode);
       if (result != LE_OK) {
           LE_ERROR("Failed to set station mode: %d", result);
           return result;
       }
 *
       // Verify the mode was set
       taf_wlanSta_Mode_t currentMode;
       result = taf_wlanSta_GetMode(staRef, &currentMode);
       if (result == LE_OK) {
           const char* modeStr = (currentMode == TAF_WLANSTA_MODE_ROUTER) ?
                                 "Router" : "Bridge";
           LE_INFO("Station mode set to: %s", modeStr);
       }
 *
       return result;
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_disconnect Manage disconnections
 *
 * Properly disconnect from an access point:
 *
 * @code
 *
   le_result_t DisconnectFromAP(taf_wlanSta_WlanSTARef_t staRef, const char* ssid)
   {
       le_result_t result;
 *
       // Get current scan results to find the AP info
       uint16_t numAPs = 0;
       size_t apInfoSize = TAF_WLANSTA_MAX_APSCAN_RESULT_NUM;
       taf_wlanSta_APInfo_t apInfo[TAF_WLANSTA_MAX_APSCAN_RESULT_NUM];
       taf_wlanSta_APInfo_t targetAP;
       bool apFound = false;
 *
       result = taf_wlanSta_GetAPScanResults(staRef, &numAPs, apInfo, &apInfoSize);
       if (result == LE_OK) {
           for (size_t i = 0; i < numAPs && i < apInfoSize; i++) {
               if (strcmp(apInfo[i].SSID, ssid) == 0) {
                   targetAP = apInfo[i];
                   apFound = true;
                   break;
               }
           }
       }
 *
       if (!apFound) {
           LE_WARN("Target AP '%s' not found in scan results, using current connection", ssid);
           // For disconnect, we can still proceed without exact AP info
           memset(&targetAP, 0, sizeof(targetAP));
           strncpy(targetAP.SSID, ssid, sizeof(targetAP.SSID) - 1);
       }
 *
       // Disconnect from the access point
       result = taf_wlanSta_Disconnect(staRef, &targetAP);
       if (result != LE_OK) {
           LE_ERROR("Failed to disconnect from AP: %d", result);
           return result;
       }
 *
       LE_INFO("Disconnection initiated for AP: %s", ssid);
       return LE_OK;
   }
 *
   @endcode
 *
 * @section c_taf_wlan_sta_status_monitoring Monitor station and connection status
 *
 * Monitor station status and connection information:
 *
 * @code
 *
   void MonitorStationStatus(taf_wlanSta_WlanSTARef_t staRef)
   {
       taf_wlanSta_State_t state;
       char intfName[TAF_NET_INTERFACE_NAME_MAX_LEN];
       char ipv4Addr[TAF_NET_IPV4_ADDR_MAX_LEN];
       char ipv6Addr[TAF_NET_IPV6_ADDR_MAX_LEN];
       char macAddr[TAF_NET_MAC_ADDR_MAX_LEN];
 *
       le_result_t result = taf_wlanSta_GetStatus(staRef, &state, intfName, sizeof(intfName),
                                                  ipv4Addr, sizeof(ipv4Addr), ipv6Addr,
                                                  sizeof(ipv6Addr), macAddr, sizeof(macAddr));
 *
       if (result == LE_OK) {
           LE_INFO("Station Status:");

           // Print state
           switch (state) {
               case TAF_WLANSTA_STATE_UNKNOWN:
                   LE_INFO("  State: UNKNOWN");
                   break;
               case TAF_WLANSTA_STATE_CONNECTING:
                   LE_INFO("  State: CONNECTING");
                   break;
               case TAF_WLANSTA_STATE_CONNECTED:
                   LE_INFO("  State: CONNECTED");
                   break;
               case TAF_WLANSTA_STATE_DISCONNECTED:
                   LE_INFO("  State: DISCONNECTED");
                   break;
               case TAF_WLANSTA_STATE_ASSOCIATION_FAILED:
                   LE_INFO("  State: ASSOCIATION_FAILED");
                   break;
               case TAF_WLANSTA_STATE_IP_ASSIGNMENT_FAILED:
                   LE_INFO("  State: IP_ASSIGNMENT_FAILED");
                   break;
               default:
                   LE_INFO("  State: %d", state);
                   break;
           }
 *
           LE_INFO("  Interface: %s", intfName);
           LE_INFO("  IPv4 Address: %s", ipv4Addr);
           LE_INFO("  IPv6 Address: %s", ipv6Addr);
           LE_INFO("  MAC Address: %s", macAddr);
 *
           // Get IP configuration details
           taf_wlanSta_IPType_t ipType;
           taf_wlanSta_IPConfig_t staticConfig;
           result = taf_wlanSta_GetIPConfig(staRef, &ipType, &staticConfig);
           if (result == LE_OK) {
               LE_INFO("  IP Type: %s", (ipType == TAF_WLANSTA_IPTYPE_DYNAMIC) ?
                                       "Dynamic (DHCP)" : "Static");
               if (ipType == TAF_WLANSTA_IPTYPE_STATIC) {
                   LE_INFO("  Static Config:");
                   LE_INFO("    IP: %s", staticConfig.IPv4Addr);
                   LE_INFO("    Gateway: %s", staticConfig.GWAddr);
                   LE_INFO("    DNS: %s", staticConfig.DNSAddr);
                   LE_INFO("    Netmask: %s", staticConfig.NetMask);
               }
           }
       } else {
           LE_ERROR("Failed to get station status: %d", result);
       }
   }
 *
   @endcode
 *
 * @section taf_wlansta_usage_flow Typical application call flow
 *
 * @rstblock
 * - **Station connection workflow**
 *
 *   1. Initialize WLAN device and set STA mode.
 *
 *   #. Get STA interface information and obtain STA reference.
 *
 *   #. Register event handler for connection state monitoring.
 *
 *   #. Start the station and configure IP settings.
 *
 *   #. Scan for available access points.
 *
 *   #. Set authentication credentials for target AP.
 *
 *   #. Connect to the target access point.
 *
 *   #. Monitor connection status and handle events.
 *
 *   #. Disconnect and cleanup when done.
 *
 * @endrst
 *
 * <HR>
 *
 */
//--------------------------------------------------------------------------------------------------

API_VERSION = 1;
SERVING_VERSION = 1;

USETYPES taf_wlan.api;
USETYPES taf_net.api;

//--------------------------------------------------------------------------------------------------
/**
 * Maximum number of AP scan results that will be returned.
 */
//--------------------------------------------------------------------------------------------------
DEFINE MAX_APSCAN_RESULT_NUM = 24;

//--------------------------------------------------------------------------------------------------
/**
 * Maximum number of clients that can register for connected AP signal strength events.
 */
//--------------------------------------------------------------------------------------------------
DEFINE MAX_CONNECTED_AP_SIGNAL_STRENGTH_CLIENTS = 2;

//--------------------------------------------------------------------------------------------------
/**
 * WLAN STA status.
 */
//--------------------------------------------------------------------------------------------------
ENUM State
{
    STATE_UNKNOWN,              ///< Station interface is unknown.
    STATE_CONNECTING,           ///< Station interface is connecting.
    STATE_CONNECTED,            ///< Station interface is connected.
    STATE_DISCONNECTED,         ///< Station interface is disconnected.
    STATE_ASSOCIATION_FAILED,   ///< Station is unable to associate with AP.
    STATE_IP_ASSIGNMENT_FAILED, ///< Station is unable to get IP address via DHCP.
    STATE_SCAN_STARTED,         ///< Available access points scan started.
    STATE_SCAN_COMPLETED,       ///< Available access points scan is complete.
    STATE_SCAN_FAILED,          ///< Available access points scan failed.
    STATE_NETWORK_REMOVED       ///< Station network is removed successfully
};

//--------------------------------------------------------------------------------------------------
/**
 * WLAN STA mode.
 */
//--------------------------------------------------------------------------------------------------
ENUM Mode
{
    MODE_UNKNOWN=-1,   ///< To skip setting encryption method, pass UNKNOWN.
    MODE_ROUTER,       ///< Router mode.
    MODE_BRIDGE        ///< Bridge mode.
};

//--------------------------------------------------------------------------------------------------
/**
 * WLAN STA IP address type.
 */
//--------------------------------------------------------------------------------------------------
ENUM IPType
{
    IPTYPE_UNKNOWN=-1,   ///< To skip setting encryption method, pass UNKNOWN.
    IPTYPE_DYNAMIC,      ///< Station is configured with dynamic IP.
    IPTYPE_STATIC        ///< Station is configured with Static IP.
};

//--------------------------------------------------------------------------------------------------
/**
 * WLAN STA IP configuration.
 */
//--------------------------------------------------------------------------------------------------
STRUCT IPConfig
{
    string IPv4Addr[taf_net.IPV4_ADDR_MAX_LEN];    ///< IPv4 address to be assigned.
    string GWAddr[taf_net.IPV4_ADDR_MAX_LEN];      ///< IPv4 address of the gateway.
    string DNSAddr[taf_net.IPV4_ADDR_MAX_LEN];     ///< DNS IPv4 address.
    string NetMask[taf_net.IPV4_ADDR_MAX_LEN];     ///< Subnet mask.
};

//--------------------------------------------------------------------------------------------------
/**
 * Information about available Access Points that were found during a scan.
 */
//--------------------------------------------------------------------------------------------------
STRUCT APInfo
{
    string  BSSID[taf_wlan.MAX_BSSID_LENGTH];              ///< BSSID of the AP.
    string  SSID[taf_wlan.MAX_SSID_LENGTH];                ///< SSID of the AP.
    int16 SignalLevel;                                     ///< Signal level in dBm.
    uint16 Frequency;                                      ///< Frequency in MHz.
    taf_wlan.ServiceSet SS;                                ///< Service Set.
    taf_wlan.SecurityMode secMode;                         ///< Security Mode.
    taf_wlan.SecurityAuthMethod secAuthMethod;             ///< Security Authentication Method.
    taf_wlan.SecurityEncryptionMethod secEncryptionMethod; ///< Security Encryption Method.
    bool WPSEnabled;                                       ///< TRUE - WPS is enabled.
                                                           ///< FALSE - WPS is not enabled.
};

//--------------------------------------------------------------------------------------------------
/**
 * WLAN STA reference to be used with WLAN STA service functions.
 */
//--------------------------------------------------------------------------------------------------
REFERENCE WlanSTA;

//--------------------------------------------------------------------------------------------------
/**
 * The WLAN STA event handler.
 *
 * @param[in] wlanSTARef   The WLAN STA reference.
 * @param[in] staState     The WLAN STA state.
 *
 * @instaging
 *
 * @param[in] wlanSTARef The WLAN STA reference.
 * @param[in] staState   WLAN STA states.
 * @param[in] contextPtr Context pointer.
 */
//--------------------------------------------------------------------------------------------------
HANDLER Handler
(
    WlanSTA wlanSTARef IN, ///< The WLAN STA reference.
    State staState     IN  ///< The WLAN STA state.
);

//--------------------------------------------------------------------------------------------------
/*
 * STA events to report.
 *
 * @instaging
 *
 */
//--------------------------------------------------------------------------------------------------
EVENT Event
(
    WlanSTA wlanSTARef IN, ///< The WLAN STA reference.
    Handler handler IN
) = (0,1);

//--------------------------------------------------------------------------------------------------
/**
 * Connected AP signal strength event handler.
 *
 * @param[in] wlanSTARef         The WLAN STA reference.
 * @param[in] signalStrength     The signal strength in dBm.
 *
 * @instaging
 *
 */
//--------------------------------------------------------------------------------------------------
HANDLER ConnectedApSignalStrengthHandler
(
    WlanSTA  wlanSTARef     IN,  ///< The WLAN STA reference.
    int16    signalStrength IN   ///< The signal strength in dBm.
);

//--------------------------------------------------------------------------------------------------
/**
 * Connected AP signal strength event. When STA is connected to an AP, clients can register to this
 * event to receive the signal strength of the connected AP.
 *
 * The handler will be called when the signal strength of the connected AP drops below the
 * configured threshold at the configured frequency. The service will not trigger the event after
 * the signal strength goes above the threshold.
 *
 * If bAverage is true, the average of signal strength over the frequency is reported. The service
 * uses the average signal strength to stop triggering the event once the events are triggered.
 *
 * If bAverage is false, the most recent signal strength value is reported.
 *
 * The maximum number of clients that can register for this event is controlled by
 * TAF_WLANSTA_MAX_CONNECTED_AP_SIGNAL_STRENGTH_CLIENTS. The default value is 2.
 *
 * @instaging
 *
 */
//--------------------------------------------------------------------------------------------------
EVENT ConnectedApSignalStrength
(
    WlanSTA  wlanSTARef      IN,  ///< The WLAN STA reference.
    int16    signalThreshold IN,  ///< The signal threshold below which the handler will be called.
                                  ///< Values outside 0 and -100, will default to -70.
    uint16   frequency       IN,  ///< The frequency, in seconds, at which the handler will be
                                  ///< called.
                                  ///< The default minimum frequency is 5 sec and maximum is
                                  ///<  300 sec. Any other value will be set to the default.
    bool     bAverage        IN,  ///< If true, averages the signal strength values over frequency.
                                  ///< If false, reports the most recent signal strength value.
    ConnectedApSignalStrengthHandler handler IN ///< The event handler.
) = (2,3);

//--------------------------------------------------------------------------------------------------
/*
 * APIs available in WLAN Access Point Management service.
 */
//--------------------------------------------------------------------------------------------------

//--------------------------------------------------------------------------------------------------
/**
 * Returns the WLAN STA reference.
 *
 * @instaging
 *
 * @return
 * - Valid reference on success.
 *
 */
//--------------------------------------------------------------------------------------------------
FUNCTION WlanSTA GetWlanSTA
(
    taf_wlan.STAid STAid IN,                                ///< STA identifier.
    string STAIntfName[taf_net.INTERFACE_NAME_MAX_LEN] IN   ///< STA associated host interface name.
) = 4;

//--------------------------------------------------------------------------------------------------
/**
 * Starts the specified station.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t Start
(
    WlanSTA wlanSTARef   IN     ///< The WLAN STA reference.
) = 5;

//--------------------------------------------------------------------------------------------------
/**
 * Stops the specified station.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t Stop
(
    WlanSTA wlanSTARef   IN     ///< The WLAN STA reference.
) = 6;

//--------------------------------------------------------------------------------------------------
/**
 * Restarts the specified station.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t Restart
(
    WlanSTA wlanSTARef   IN     ///< The WLAN STA reference.
) = 7;

//--------------------------------------------------------------------------------------------------
/**
 * Sets the station to Bridged or Router mode.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t SetMode
(
    WlanSTA wlanSTARef   IN,     ///< The WLAN STA reference.
    Mode    StaMode      IN      ///< The WLAN STA mode to set.
) = 8;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the station mode.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetMode
(
    WlanSTA wlanSTARef   IN,     ///< The WLAN STA reference.
    Mode    StaMode      OUT     ///< The WLAN STA mode that is set.
) = 9;

//--------------------------------------------------------------------------------------------------
/**
 * Sets the IP configuration of the station.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t SetIPConfig
(
    WlanSTA        wlanSTARef         IN,     ///< The WLAN STA reference.
    IPType         StaIPType          IN,     ///< Dynamic or static IP address.
    IPConfig       StaIPConfig        IN      ///< IP address to set for static IP address mode.
) = 10;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the IP configuration of the station.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetIPConfig
(
    WlanSTA        wlanSTARef         IN,     ///< The WLAN STA reference.
    IPType         StaIPType          OUT,    ///< Dynamic or Static IP address.
    IPConfig       StaStaticIPConfig  OUT     ///< Details of static IP configuration.
) = 11;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the station state.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetStatus
(
    WlanSTA wlanSTARef                                IN,  ///< The WLAN STA reference.
    State   staState                                  OUT, ///< Station state.
    string  IntfName[taf_net.INTERFACE_NAME_MAX_LEN]  OUT, ///< Associated host interface name.
    string  IPv4Address[taf_net.IPV4_ADDR_MAX_LEN]    OUT, ///< Associated IPv4 address.
    string  IPv6Address[taf_net.IPV6_ADDR_MAX_LEN]    OUT, ///< Associated IPv6 address.
    string  MACAddress[taf_net.MAC_ADDR_MAX_LEN]      OUT  ///< Associated MAC address.
) = 12;

//--------------------------------------------------------------------------------------------------
/**
 * Scan for available access points. This is an asynchronous operation and will trigger event
 * STATE_SCAN_COMPLETE on completion.
 * Use GetAPScanResults to get the results of the scan.
 * When a scan is started, it will clear the results from earlier scans.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t DoAPScan
(
    WlanSTA wlanSTARef IN                               ///< The WLAN STA reference.
) = 13;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the list of scanned access points. This list is updated after every call to DoScan.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetAPScanResults
(
    WlanSTA wlanSTARef IN,                      ///< The WLAN STA reference.
    uint16  numAP      OUT,                     ///< Number of APs found in the scan.
    APInfo  apInfo[MAX_APSCAN_RESULT_NUM] OUT   ///< Available AP information.
) = 14;

//--------------------------------------------------------------------------------------------------
/**
 * Sets WPA2 PSK for an access point that the WLAN station should connect with.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t SetWpa2Psk
(
    WlanSTA wlanSTARef IN,                              ///< The WLAN STA reference.
    APInfo  apInfo IN,                                   ///< AP for which PSK needs to be set.
    string Wpa2Psk[taf_wlan.MAX_PASSPHRASE_LENGTH] IN   ///< PSK to use for authentication.
) = 15;

//--------------------------------------------------------------------------------------------------
/**
 * Connects to an AP. This is an asynchronous operation and will trigger event STATE_CONNECTED
 * on successful connection or STATE_ASSOCIATION_FAILED if association fails.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t Connect
(
    WlanSTA wlanSTARef IN,                              ///< The WLAN STA reference.
    APInfo  apInfo IN                                    ///< The AP to connect to.
) = 16;

//--------------------------------------------------------------------------------------------------
/**
 * Disconnects from an AP. This is an asynchronous operation and will trigger event
 * STATE_DISCONNECTED on completion.
 *
 * @instaging
 *
 * @return
 * - LE_OK  -- Succeeded.
 * - Others -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t Disconnect
(
    WlanSTA wlanSTARef IN,                              ///< The WLAN STA reference.
    APInfo  apInfo IN                                   ///< The AP to disconnect from.
) = 17;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the latest signal strength of the connected AP in dBm.
 *
 * @instaging
 *
 * @return
 * - LE_OK           -- Succeeded.
 * - LE_UNAVAILABLE  -- The STA is not connected to an access point.
 * - Others          -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetConnectedApSignalStrength
(
    WlanSTA wlanSTARef         IN,   ///< The WLAN STA reference.
    int16   signalStrength     OUT  ///< The signal strength of the connected AP in dBm.
) = 18;

//--------------------------------------------------------------------------------------------------
/**
 * Gets the estimated throughput of an access point.
 *
 * @instaging
 *
 * @return
 * - LE_OK           -- Succeeded.
 * - LE_NOT_FOUND    -- The specified AP was not found in scan results.
 * - LE_UNAVAILABLE  -- Throughput information is not available for the AP.
 * - Others          -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t GetAPEstimatedThroughput
(
    WlanSTA wlanSTARef IN,                       ///< The WLAN STA reference.
    string  BSSID[taf_wlan.MAX_BSSID_LENGTH] IN, ///< BSSID of the AP.
    uint32  estimatedThroughput OUT,             ///< Estimated throughput in Kbps.
    int32   age OUT                              ///< Age of the measured value in seconds.
                                                 ///< A negative age indicates it's not available.
) = 19;

//--------------------------------------------------------------------------------------------------
/**
 * Removes a network configuration from the station. If the station is currently connected
 * to this network, it will be disconnected first before removing the network configuration.
 *
 * @b NOTE: This does not save the configuration persistently. Use SaveNetworkConfig() to save.
 *
 * @instaging
 *
 * @return
 * - LE_OK          -- Succeeded.
 * - LE_NOT_FOUND   -- Network not found in configured networks.
 * - Others         -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t RemoveNetwork
(
    WlanSTA wlanSTARef   IN,     ///< The WLAN STA reference.
    APInfo  apInfo       IN      ///< The AP network to remove.
) = 20;

//--------------------------------------------------------------------------------------------------
/**
 * Saves the current network configuration persistently. This should be called after
 * network configuration changes to make the changes persistent across reboots.
 *
 * @instaging
 *
 * @return
 * - LE_OK          -- Succeeded.
 * - LE_FAULT       -- Failed to save configuration.
 * - Others         -- Failed.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t SaveNetworkConfig
(
    WlanSTA wlanSTARef   IN      ///< The WLAN STA reference.
) = 21;
